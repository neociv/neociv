use bevy::prelude::*;
use neociv_state::state::NeocivState;

pub fn grid_startup(mut commands: Commands, state: Res<NeocivState>) {
    // TODO: Generate the grid itself and the individual (w/ ids) cells
}